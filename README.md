# codeli

Monorepo containing `client/` (Next.js) and `server/` (Express).

Quick start

Client:

```bash
cd client
npm install
npm run dev
```

Server:

```bash
cd server
npm install
npm run dev
```

Repository created and pushed by automation.
